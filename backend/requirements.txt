fastapi==0.115.0
uvicorn[standard]==0.52.4
pymongo==4.8.0
python-dotenv==1.0.1
pydantic==2.9.2
PyJWT==2.9.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
cloudinary==1.41.0
requests==2.32.3
qrcode[pil]==7.4.2
reportlab==4.2.5
apscheduler==3.10.4
pydantic[email]==2.9.2
slowapi==0.1.9
# Required by fastapi.testclient.TestClient (used in backend/tests/) since
# Starlette's TestClient delegates to httpx under the hood. Not needed by
# the running app itself -- only by the test suite / CI.
httpx==0.27.2
# Optional: only needed if REDIS_URL is set (shared DDoS-protection state
# across uvicorn --workers / multiple containers). App runs fine without it
# using per-worker in-memory state -- see app/middleware/ddos_store.py.
redis==5.0.8