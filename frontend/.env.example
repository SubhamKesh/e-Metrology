# Base URL of the MaapSetu API, without trailing slash.
# The API contract already prefixes routes with /api/v1, so point this at
# the server root, e.g. http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
